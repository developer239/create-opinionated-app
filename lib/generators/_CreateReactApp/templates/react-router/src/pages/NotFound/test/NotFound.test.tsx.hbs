import React from 'react'
import 'jest-styled-components'
import { App } from 'App'
import { renderPage } from 'testUtils/render'
import { NOT_FOUND_TEST_ID, NOT_FOUND_ROUTE } from '../index'

describe('[pages] NotFoundPage', () => {
  it('should render correctly', () => {
    const renderer = renderPage(<App />, NOT_FOUND_ROUTE)
    const renderedElement = renderer.getByTestId(NOT_FOUND_TEST_ID)
    expect(renderedElement).toBeTruthy()
  })
})
