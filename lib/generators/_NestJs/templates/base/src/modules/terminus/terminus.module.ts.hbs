import { Module } from '@nestjs/common'
import { TerminusModule as TerminusModuleOriginal } from '@nestjs/terminus'
import { TerminusOptionsService } from 'src/modules/terminus/terminus-options.service'

@Module({
  imports: [
    TerminusModuleOriginal.forRootAsync({
      useClass: TerminusOptionsService,
    }),
  ],
})
export class TerminusModule {}
